evaluator:
  type: "trec"  # or other evaluator types we might add in future
  model:
    type: "openai"
    name: "gpt-4o-mini"
    api_key: ${oc.env:OPENAI_API_KEY}  # reads from environment variable